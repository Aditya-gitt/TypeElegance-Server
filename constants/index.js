//Random constants
const CLIENT_ORIGIN_URL = "http://localhost:3000";
const CORS_OPTIONS = {
  origin: CLIENT_ORIGIN_URL,
  optionsSuccessStatus: 200,
  credentials: true,
};
module.exports = {
  CLIENT_ORIGIN_URL,
  CORS_OPTIONS,
};
